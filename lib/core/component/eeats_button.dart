import 'package:flutter/material.dart';

class EeatsButton extends StatelessWidget {
  const EeatsButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
